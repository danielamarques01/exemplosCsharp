using System;

namespace exemplo
{
    class Program
    {
        static void Main(string[] args)
        {
            int x, y;

            x = 5;
            y = 2 * x;
            Console.WriteLine(x);
            Console.WriteLine(y);

            //

           double b, B, h, area;

            b = 6.0;
            B = 8.0;
            h = 5.0;

            area = (b + B) / 2.0 * h;

            //

            Console.WriteLine(area);

            int c, d;
            double resultado;
            c = 5;
            d = 2;

            resultado = c / d;

            Console.WriteLine(resultado);

            //

            double e;
            int f;

            e = 5.0;
            f = (int) e;


            Console.WriteLine(f);
        }
    }

}
